# Required Environment Variables for REZ Price Predictor

# MongoDB Connection
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/rez_price_predictor

# Redis Connection
REDIS_URL=redis://localhost:6379

# Internal Service Token (generate with: openssl rand -hex 32)
INTERNAL_SERVICE_TOKEN=change-me-to-secure-random-string

# Service Configuration
PORT=4043
NODE_ENV=development
LOG_LEVEL=info
SERVICE_NAME=rez-price-predictor

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://rez.money

# Optional: Sentry for error tracking
SENTRY_DSN=
